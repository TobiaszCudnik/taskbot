System.register("../../../node_modules/tracery/diff", [], function() {
  "use strict";
  var __moduleName = "../../../node_modules/tracery/diff";
  var is = require('./is');
  function diff(Interface, doc) {
    var d = {};
    var same = true;
    for (var prop in Interface) {
      var actual = doc[prop];
      var expected = Interface[prop];
      var test = is(actual);
      if (!test) {
        var dd = diff(expected, actual);
        if (dd) {
          d[prop] = dd;
        }
      } else if (!is(expected)(actual)) {
        same = false;
        d[prop] = {
          actual: toString(actual),
          expected: toString(expected),
          actualValue: actual
        };
      }
    }
    return same ? false : d;
  }
  function toString(type) {
    var t = typeof type;
    if (t === 'function') {
      return type.name || 'Custom Function';
    }
    if (t !== 'object')
      return t[0].toUpperCase() + t.substring(1);
    if (Array.isArray(type)) {
      var t0 = toString(type[0]);
      if (type.every(function(ele) {
        return toString(ele) === t0;
      })) {
        return 'Array<' + t0 + '>';
      } else {
        return 'Array';
      }
    }
    return Object.prototype.toString(type).replace(/[\[\]]/g, '');
  }
  module.exports = diff;
  return {};
});
System.get("../../../node_modules/tracery/diff" + '');
